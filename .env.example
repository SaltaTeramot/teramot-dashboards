# Ruta local al repo clonado
GITHUB_REPO_PATH=/ruta/al/repo/teramot-dashboards

# URL base de GitHub Pages
GITHUB_PAGES_URL=https://saltateramot.github.io/teramot-dashboards

# AWS — usadas tanto por el MCP local como por GitHub Actions (via Secrets)
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AWS_DEFAULT_REGION=us-east-1

# Bucket S3 donde Athena deposita los resultados de las queries
ATHENA_OUTPUT_BUCKET=teramot-athena-results

# ── MCP HTTP Server (para exposición remota via ngrok) ──────────────────────
# Puerto local donde corre el MCP server en modo HTTP/SSE
MCP_PORT=8080
